function libUtils
% This library has miscellaneous functions. 


% Here is the list of functions:
% ecogInd=TimeAlign(eventTime,ecogTimeStamp)
% [Pc,I] = getITRC(Pyc,Pc)
% I = calcMI(Pyc,Pc)
% Trials = getEpochs(data, Labels,fs, tau,delay,numbins, groupDelay)
% featRanks =  getFeatRanks(Trials, method)
% f= plotCM(CM, toNorm,groupNames)
% f=showSpectrograms(data,fs,timeWin,params,toZscore)
% plotHist(Trials,Dims)
%  f= getRankPlots(ranks,thresh)



% Other downloaded util functions
%  [d,fs,dt,tc,t]=readhtk(file,duration)
% barweb()
% shadedErrorBars()


% All of them return figure handles

% Author : Tejaswy Pailla,
% Date   : 5/30/2016



end